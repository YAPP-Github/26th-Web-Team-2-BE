CREATE TABLE trip_board
(
    id         BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    group_name VARCHAR(255),
    created_by BIGINT,
    updated_by BIGINT,
    created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT pk_trip_board PRIMARY KEY (id)
);

ALTER TABLE trip_board
    ADD CONSTRAINT FK_TRIP_BOARD_ON_CREATED_BY FOREIGN KEY (created_by) REFERENCES users (id);

ALTER TABLE trip_board
    ADD CONSTRAINT FK_TRIP_BOARD_ON_UPDATED_BY FOREIGN KEY (updated_by) REFERENCES users (id);